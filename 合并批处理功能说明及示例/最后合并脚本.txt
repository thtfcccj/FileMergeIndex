ccj bin文件合并描述脚本V1.00; //首行为描述识别符，以下所有行以“;”结尾,起始位置必须(十六进制)顺序排列
0xff;                        //第二行指定未被填充区域(字节为单位)的填充字符(一般为0xff)，十六进制表示
0x0001800;                  //第三行指定生成文件的大小，十六进制表示
0x0000100,bMarge.bin;       //第四行起， 指定需合并文件在合并后文件中的起始位置(十六进制)与文件名
0x0000C00,uiGlobal.Bin;     //若中间需要为将来预留空间，则填充空格; 此时不处理
0x0000E00,中间资源.bin;      //继续
;                            //空表示文件结束了,中止处理