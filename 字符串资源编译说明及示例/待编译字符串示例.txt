说明:
文件用于转换示例，同时兼说明功能：
1. 以行为单位进行编译
2. 每行起始位置将在编译完成的文件头部，将产生一个索引数组
3. 程序中以索引数组查找索引值，再根据索此值偏移量，即可找到此字符串
4. 编译成的字符串取消了此txt文件的回字换行符，以节省空间
5. 暂不支持转义字符，如\\,\n,\0
6. 编译完成的文件大小由此文件确定，故建议将共放入固定的程序位置，或继续合用资源文件子模块进行合并。
END